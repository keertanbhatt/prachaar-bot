export function DemoBanner() {
  return (
    <div className="demo-banner">
      <strong>BOCW Scheme Assistant</strong>
      <span>
        Standalone demo — runs in your browser (no server). Real WhatsApp uses the
        backend when deployed.
      </span>
    </div>
  );
}
